package JsonConfig;

use strict;
use Exporter;
use JSON;
use vars qw($VERSION);
$VERSION = 0.1;

sub new {
	my ($class, $file) = @_;
}

#TODO

sub get {
	my ($self, $key, $default) = @_;
}

sub set {
	my ($self, $key, $value) = @_;
}

return 1;